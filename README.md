# DE5-M5-20251110

Author: Nirosh Suthagar

Generating an ssh-key and linking with GitHub:
```
ssh-keygen
cd .ssh #or alternative path to .ssh
cat id____.pub
```
Copying the output of cat, paste it under GitHub, Settings, SSH and GPG Keys as a new key.
```
Docker commands to remember:
docker build -t container-name .
docker run container-name

docker images # for all images
docker ps -a #for all containers
```